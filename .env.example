# Optional. When set, hover translations use Claude for context-aware meanings.
# Without it, the free MyMemory API is used (no key required).
ANTHROPIC_API_KEY=

# Optional. Force a translation backend: claude | mymemory
TRANSLATE_PROVIDER=

# Optional. Raises MyMemory's daily quota when set to your email address.
MYMEMORY_EMAIL=
